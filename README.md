# Learning Companion Agent на Python с LangGraph и GigaChat

## Описание
AI-агент помогает обучаться выбранной теме с сохранением контекста и адаптацией под пользователя.

## Архитектура
- LangGraph для управления диалогом
- sqlite3 для состояния
- Векторная память Chroma с RAG-подходом загрузка из PDF
- LLM GigaChat для генерации ответов

## Клонировать и запустить

- pip install -r requirements.txt
- cp .env.example .env # Добавить GIGACHAT_TOKEN, указать каталог размещения
- python AgentRAG.py

## Docker
docker build -t learning-agent .
docker run -it learning-agent


## Основная структура проекта
```
/
    |-- state_db
        |-- Python-ed2.pdf    # Справочник в формате pdf по Python
    |-- AgentRAG.py           # Основной код агента с LangGraph и GigaChat
    |-- tools_rag_pdf.py      # Модель памяти с БД и векторной RAG
    |-- tools_prompts.py      # Опписание промтов
    |-- tools_png_graph.py    # Визуализация графа в PNG
    |-- tools_llms.py         # Описание модели Gigachat
    |-- .env                  # Описание токенов к модели Gigachat
    |-- requirements.txt      # Зависимости Python
    |-- Dockerfile            # Контейнеризация проекта
    |-- README.md             # Документация проекта
```

## Тест
ИИ-агент с долговременной памятью.
Для выхода напишите 'exit'.

Enter: Привет
================================== Ai Message ==================================

👋 Привет! Рад тебя видеть здесь.

Давай начнем с самого простого. Сегодня мы поговорим о том, как пользоваться программой на языке Python.

⚙️ Вот самое простое, что можно сделать в Python - это напечатать текст.

**Пример 1 — печать текста:**
```python
# ← Печать приветствия
print("Привет!")
```

Попробуй сам!
Напиши простую программу, которая скажет тебе "Добро пожаловать!" 😊
------ Конец ответа -----

Enter: загрузи документ
Загрузка документа в RAG
Loaded 188 page(s) from ../SberRag/state_db/Python-ed2.pdf
Split into 876 chunks
Документы загружены в RAG
================================== Ai Message ==================================

📚 Давай разберем, как загружать документы в Python!

Представим, что у нас есть список студентов и мы хотим сохранить его в специальном формате, который называется «пикл» (`pickle`).

Вот простая программа, которая сохраняет список студентов в файл:

```python
# ← Сохраняем список студентов в файл
students = ["Иван", "Анна", "Сергей"]

import pickle

with open('бд_студенты.pickle', 'wb') as f:
    pickle.dump(students, f)
```

Здесь мы используем функцию `pickle.dump()` для сохранения данных в файл.

Теперь давай посмотрим, как прочитать этот файл обратно:

```python
# ← Чтение сохраненного списка студентов из файла
import pickle

with open('бд_студенты.pickle', 'rb') as f:
    students = pickle.load(f)

print(students)  # выведет ['Иван', 'Анна', 'Сергей']
```

Здесь функция `pickle.load()` читает данные из файла и возвращает их в виде списка.

Попробуй сам!
✨ Попробуй создать свой собственный список и сохранить его в файл. Потом прочитай его обратно.
------ Конец ответа ------
